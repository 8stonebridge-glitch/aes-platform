import type { AESStateType } from "../state.js";

export async function builderDispatcher(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - dispatches build job for current feature
  return {};
}
