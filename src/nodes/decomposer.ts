import type { AESStateType } from "../state.js";

export async function decomposer(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - decomposes confirmed intent into AppSpec with features
  return {};
}
