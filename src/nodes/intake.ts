import type { AESStateType } from "../state.js";

export async function intake(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - receives raw request and prepares it for classification
  return {};
}
