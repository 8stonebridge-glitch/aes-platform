import type { AESStateType } from "../state.js";

export async function intentClassifier(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - classifies user intent into structured brief
  return {};
}
