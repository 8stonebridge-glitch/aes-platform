import type { AESStateType } from "../state.js";

export async function intentConfirmer(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - confirms intent with user before proceeding
  return {};
}
