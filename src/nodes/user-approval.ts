import type { AESStateType } from "../state.js";

export async function userApproval(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - pauses graph for user approval (human-in-the-loop)
  return {};
}
