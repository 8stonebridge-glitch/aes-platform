import type { AESStateType } from "../state.js";

export async function validatorRunner(state: AESStateType): Promise<Partial<AESStateType>> {
  // TODO: Implement - runs validators against build output
  return {};
}
